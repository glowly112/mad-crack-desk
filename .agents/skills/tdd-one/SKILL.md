---
name: TDD one
description: >-
  Use this when behaviour is runnable in a test runner. One failing test, watch
  it fail for the right reason, then the smallest code that passes.
---
# TDD one

One failing test. Watch it fail for the right reason. Then the smallest code that passes.

No 40-test generator. No prod code first.

Exempt pixel-only UI factory / thrift work.
